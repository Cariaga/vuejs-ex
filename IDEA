//it seams that dedicating the server to certain operation should be required
// e.g Socket-Server API-Server WebHook-Server Admin-Server-front-End Admin-SocketServer archive-backup-server this means a server failure will not crash all servers both in terms of coding error, updatablility,scalablility
// The API-server the most low level access 

//We need to archive Money when leaving and entering game